import React from "react";

import { Collapse } from "antd";

const { Panel } = Collapse;

import styles from "@/styles/Home.module.css";

const Calculator = (props) => {
  return <div style={{height: '100%'}}><Calculator /></div>;
};

export default Calculator;
